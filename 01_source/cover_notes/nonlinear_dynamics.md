# For specialists in nonlinear lattice dynamics / synthetic gauge fields

**Ask:** check Results 1–7 (spec §5.2): are the claims, controls, and
perturbation theory sound? Every script reproduces its result in about a
minute (`README.md`).

Highlights: gyroscopic velocity coupling produces an exact synthetic U(1)
field (stiffness-free law Δω = −2cβ sin k); for matrix-valued coupling the
zero-power condition selects the Hermitian class and non-commuting
transport is generated with no link variables (59.86° vs 59.84°
predicted). A direction-selective decay window is located by second-order
PT including product cross-softening; in the unified mass-carrying model
(s4_*) the window is dissolved by the alignment gap — both results carry
their scope honestly. One correction is documented: an earlier coefficient
retracted as a preparation bias that passed parity checks (spec Result 2)
— the diagnostic chain is in `phi_gauge_delta.py`.
