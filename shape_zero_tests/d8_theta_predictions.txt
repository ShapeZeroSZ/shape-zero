WHAT FIXES theta_ab? -- HYPOTHESES AND PREDICTIONS (committed before any test is run)
2026-09-26. Tests will be in shape_zero_tests/d8_theta.py; nothing below has been computed.

Background (recorded): the D8 flow psi' = psi a + b psi, M = R_a + L_b, has frequencies
{0, 2 sin(theta/2), 2}; ratio 1/sin(theta/2) (d8_closed_form.py). It was derived in
z1_d8_flow.py as "the minimal conservative two-generator law" from conservativity,
minimality and plurality, with a, b drawn as independent random unit imaginaries
(z1_d8_plurality.py); plurality forbids only b = +-a.

(1) ORTHOGONALITY -- is theta = 90 deg (ratio sqrt2) required?
 H1  Nothing in the model's structure requires a . b = 0.
 O1  (Artin) for every theta in (0, 180) deg the subalgebra generated by a and b is
     4-dimensional and associative (a quaternion subalgebra, i.e. a Fano line up to G2);
     it is 2-dimensional only at theta = 0, 180. So "a, b lie on an associative triple"
     holds at EVERY angle; orthogonality is not needed for it. Predicted: dim 4 and
     associator < 1e-12 on it at all sampled theta in (0, 180).
 O2  the octonionic content of the generic flow (z1_d8_flow's measures: the dimension of
     the swept span, and the associator on it) is the same at every theta in (0, 180),
     for a generic psi_0; it changes only at 0 and 180. Predicted: span dim 8 and a
     nonzero associator at every sampled theta in (0, 180).
 O3  plurality's frequency count (two distinct nonzero frequencies) holds at every theta in
     (0, 180). At rational 1/sin(theta/2) (60 deg: 2; 180 - small: near 1) the two are
     commensurate; if plurality were read as "rationally independent", it would exclude a
     measure-zero set and select nothing. 90 deg gives sqrt2 -- irrational, not special.
 O4  missions 5 and 6 (role colouring -> 7 points -> the Fano plane) constrain the
     multiplication TABLE (which triples associate), not the choice of the flow's
     generators; neither they nor the D8 scripts use a . b = 0. Predicted: a text search of
     the D8 scripts and the mission statements finds no orthogonality condition on (a, b).
 G2  (known: G2 is transitive on pairs of unit imaginaries at fixed angle) theta is the
     only invariant of (a, b). Predicted: the spectrum of M for random pairs at equal
     theta agrees to 1e-12.
 Outcome predicted for (1): theta = 90 deg is NOT derived.

(2) PERSISTENCE AS ENDURANCE -- does the model contain a nonlinear perturbation of the D8
    flow?
 H2  No. The D8 flow is exactly linear, and nothing else in the model perturbs it.
 P1  every D8 law in 04_scripts/rungs (z1_d8_flow, z1_d8_minimality, z1_d8_plurality,
     z1_d8_dynamics, z1_d8_attempt) is linear in psi; no script adds a psi-dependent
     term to psi' = psi a + b psi. (The 22 / 14 cubic four-derivative invariants of
     d8_census_clean.py are a separate census of the fibre's field data, not a term in
     this flow.)
 P2  the lattice realises only ONE octonionic generator: the residual term
     C_r mul(g, v) = C_r L_g v (MODEL_SPEC 4b.1, "reduces ... to the existing D8
     structure"). The J-sector term kappa JJ v is a second gyroscopic term; the question is
     whether JJ is a one-sided octonionic multiplication.
       H2a (predicted): JJ = L_e for a unit imaginary e of the residual's table, so
           kappa JJ + C_r L_g = L_(kappa e + C_r g) -- ONE generator; the lattice carries
           no R_a + L_b pair and no theta_ab.
       H2b (alternative): JJ = R_e -- then the n = 8 node with kappa and C_r both on carries
           kappa R_e + C_r L_g, the D8 form with |b|/|a| = C_r/kappa, inside a nonlinear
           (radial-well) second-order system. Even then its mode frequencies solve
           w^2 -+ lambda w = K (lambda the eigenvalues of the gyroscopic operator), so the
           golden ratio of the FIRST-order D8 flow's frequencies would not carry over.
       H2c: JJ is neither -- the lattice carries no D8 form.
     Test: compare JJ (n = 8, 16 x 16) with L_e and R_e for every unit e_k of the
     sedenion table d16_spectrum_v2.cd(4) the residual uses, and of its octonion part.
     Also: gate 11 runs the residual at kappa = 0; no recorded run has kappa and C_r both on.
 Outcome predicted for (2): the model contains NO nonlinear perturbation of the D8 flow.
     Then, per the request, none is added: selecting phi with an added perturbation would
     put phi in rather than find it. theta_ab stays chosen; phi at 76.3 deg stays
     "attainable, not selected".
