jcompat_q3_output.txt -- READING (written after the run; the post-hoc parts are marked)

1. SLAB (the model's geometry, model.make_links; q3_gate.py uses it). All three
   kappa as predicted: open at 0.25 (ratio fall 0.851, leak 3.2e-2 -> 2.3e-3 ~ g^2),
   closed at 1.2 and 2.3 (ratio falls x0.249 both; leak 3e-7 -> 2e-8 and
   1e-7 -> 7e-9). Weight off k_perp = (pi, pi): 2.8e-8 to 3.7e-8 with the segment,
   equal to the no-segment reference's own floor (2.8e-8 to 3.7e-8) -- transverse
   momentum is conserved to the numerical floor. kappa = 1.2 lies BELOW the
   non-conserving floor 2.091 and the channel is closed: the 2.091 bound does not
   apply to the model's segments.

2. STAGGER (control; the same links times (-1)^(y+z), which does not conserve
   k_perp). kappa = 1.2: OPEN -- eff_x 20.2 deg (g = 0.04) against 1.3 deg for the
   slab, leak 8.1e-2 against 3.5e-7, i.e. the non-conserving coupling opens a
   channel the slab keeps closed at the same kappa. kappa = 2.3: CLOSED
   (eff_x 1.89 -> 0.119, x1/16 ~ g^2; leak 7e-7).

   POST HOC, two corrections to the committed prediction and verdict lines:
   (a) The printed verdict criterion (eff_x/eff_c falls x0.25) does not apply to
       the stagger: its J-respecting segment also changes k_perp and has no open
       channel, so eff_c is itself second order (1.84 -> 0.115, x1/16) and the ratio
       of two g^2 quantities is flat whether or not the J-breaking channel is
       closed. The stagger verdicts must be read from eff_x and leak_x: closed
       when eff_x ~ g^2 and leak ~1e-7, open when leak is orders larger. On that
       reading 2.3 is closed (the printed "open" is the criterion misapplied).
   (b) The prediction "stagger open at kappa = 0.25" was WRONG: it used only the
       lower condition 2 kappa w <= Q - K and omitted the upper edge. The stagger
       reaches only k_perp = (0, 0), whose band is [K, K + 4c] = [2.236, 6.236];
       the opposite chirality needs w^2 - kappa w in that band: 10.548 at
       kappa = 0.25 (above it -- closed), 5.158 at 1.2 (inside -- open), 0.588 at 2.3
       (below -- closed). Measured: 0.25 closed (eff_x 1.92 -> 0.121, x1/16; leak
       2.5e-7), consistent with the corrected condition. The zone-wide floor 2.091
       assumes a coupling that can reach ANY k_perp; the stagger reaches one.

3. ANSWER. The coupling segments in model.py and q3_gate.py are full transverse
   slabs and conserve transverse momentum (code: make_links fills
   W[start + j, :, :]; every other term is on-site or uniform; measured: weight
   off k_perp at the reference floor). The applicable floor is the conserving
   one, kappa* = 2c/sqrt(K + 2c) = 0.971737, at q = 3 as at q = 1. Scope: full
   slabs across a periodic transverse box, amplitude 1e-3; a segment of finite
   transverse aperture would not conserve k_perp and is not what the model defines.
